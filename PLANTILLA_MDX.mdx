---
id: nombreDeLaHerramienta
titulo: Título de la guía
herramienta: Nombre de la herramienta
personaEncargada: Su nombre
fechaDeCreacion: fecha en que se hace el PR
fechaDeUltimaActualizacion: fecha en que se actualizó por última vez la entrada. Inicialmente igual a fechaDeCreacion
framework: Elija solo uno → React | Vue | Svelte | SolidJS | Preact | Astro
---

{/* importe el componente correspondiente aquí, por ejemplo: */}
import UseState from '../src/components/React/UseState';

{/* descripción breve de la herramienta */}
`useState` es un hook fundamental en React que permite a los componentes funcionales tener y gestionar su propio estado. Antes de los hooks, solo los componentes de clase podían tener estado, pero con `useState`, los componentes funcionales también pueden hacerlo de manera sencilla.

{/* indique cómo usar el componente. Agregue toda la documentación necesaria */}

## Importación

Para usar `useState`, primero debe importarlo desde React:


{/* todos los fragmentos de código deben tener esta clase */}
<div className='bloqueCodigo'>
```jsx
import React, { useState } from 'react';
```
</div>

{/* ejemplo de uso básico */}

## Uso básico

<UseState client:load />

{/* agregue más secciones si es necesario. Entre más documentación, mejor. */}
